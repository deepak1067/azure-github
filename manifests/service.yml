apiVersion: v1
kind: Service
metadata:
  name: springboot-app-service3
spec:
  type: LoadBalancer
  selector:
    app: springboot-app3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
