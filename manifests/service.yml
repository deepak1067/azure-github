apiVersion: v1
kind: Service
metadata:
  name: springboot-app-service5
spec:
  type: LoadBalancer
  selector:
    app: springboot-app5
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
