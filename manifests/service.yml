apiVersion: v1
kind: Service
metadata:
  name: springboot-app-service4
spec:
  type: LoadBalancer
  selector:
    app: springboot-app4
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
